Synthesis for skills/ux-interview: applying the union of two grounded cuts to skills/ux-interview/SKILL.md.

Review 1 and Review 2 both flag "Ask exactly one question per turn" under Interview Rules as a second statement of the rule already made under Step 2's Actions ("Ask one question at a time about goals, frequency, and usage context."), citing scripts/auditing/SKILL_REVIEW_CHECKLIST.md section 4, "a second statement of a rule already stated elsewhere in the same file." Review 1 names the Interview Rules bullet as the duplicate; Review 2 names the Step 2 bullet as the duplicate instead. Both copies sit inside the resulting union and no third statement of the rule stands outside it, so the retention test applies: the earliest-occurring copy, Step 2's Actions bullet, is retained, and every other copy in the group - here, only the Interview Rules bullet - is deleted. Applied per checklist section 4.

The pruning pass found nothing further in the closed section 4 list to cut on its own account.

Review 2 additionally proposes removing the whole "Example Output Skeleton" section as a duplicate of the Output Contract section a few headings above it, citing checklist section 4 and noting the risk that a hand-authored example drifts from the contract it illustrates. That is a whole-section removal, propose-never-execute regardless of which review names it, so it is recorded below rather than applied.

Files changed: skills/ux-interview/SKILL.md - removed the Interview Rules bullet duplicating Step 2's "ask one question at a time" rule (-1 line).

REMOVAL PROPOSALS: 1. skills/ux-interview/SKILL.md, "Example Output Skeleton" section - duplicates the Output Contract section's required structure in worked form; evidence: the two sections name the same six subsections in the same order. Would lose the worked example, which checklist section 8 also asks for, so this is a genuine trade-off for the operator rather than a clear cut.

DIFFERENTIATION: WEAK - the one-question-at-a-time interview structure is generic enough that a frontier model would likely produce it unprompted; the deterministic filename and section-order contract is the part that earns its keep.

REVIEW_STATUS: CHANGED
