# DTOs and Validation (Condensed)

- Define DTOs per endpoint.
- Use class-validator + class-transformer.
- Keep validation at the boundary (controllers).
- Strip unknown properties with `whitelist`.
