# References index

Depth behind SKILL.md. Read the file that matches the step in hand, not both.

| File | Read when |
| --- | --- |
| `record-shapes.md` | Building or reviewing the workspace: field-by-field shapes for the spec, run configuration, arm map, eval snapshot, grading record, timing record, and rollup, plus the id join, the four arithmetic checks, the arm-variable fields for an agent-versus-agent run, and per-cell agreement across repeats. |
| `threats-to-validity.md` | Deciding whether a completed run's result can be believed, or defending the design: the defect catalog behind the procedure's rules, each with what it does to a result and the guard that closes it. |
| `behavioral-compliance.md` | The graded property is what the agent did rather than what it wrote: the three-level prompt strictness ladder, the trace-assertion class and its two-pass grading, and the rule against averaging across levels. |
