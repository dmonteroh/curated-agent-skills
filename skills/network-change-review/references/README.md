# References Index

Load only the file the current task needs.

- `validation-checks.md`: runnable pattern checks for all five layers, the single block-parsing convention every section-scoped check goes through, and how each check fails — plus the function that composes the blocked/cleared verdict.
- `ios-syntax.md`: the Cisco IOS and IOS-XE syntax layer — configuration mode hierarchy, the running-versus-startup persistence rule, read-only collection commands, the wildcard mask table, ACL form, interface hygiene, and change-window verification commands.
