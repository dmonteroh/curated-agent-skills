# jira-issue-management References

- `project-map-template.md`: the format of the project-local Jira map file plus the MCP discovery procedure to bootstrap or refresh it. Read when the map is missing, stale, or being extended.
- `mcp-tool-notes.md`: per-tool parameter shapes, field value formats, JQL rules, rate limits, and verified pitfalls of issue-level Atlassian MCP toolsets. Read before composing non-trivial create/edit/search calls or when a call misbehaves.
