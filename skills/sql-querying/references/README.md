# SQL Querying References

- `query-patterns.md`: High-signal query shaping patterns (CTEs, EXISTS, anti-joins, lateral joins).
- `window-functions.md`: Ranking, running totals, and time comparison examples with common gotchas.
- `dialect-notes.md`: Dialect differences and portability reminders (limits, upserts, string concat).
- `explain-basics.md`: How to read EXPLAIN output and identify plan risks.
