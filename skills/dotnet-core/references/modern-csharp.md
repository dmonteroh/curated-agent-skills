# Modern C# Notes

- Prefer `record` for immutable DTOs.
- Use `required` for required init properties.
- Favor `async` streams when handling large datasets.
- Use `using` declarations for proper disposal.
