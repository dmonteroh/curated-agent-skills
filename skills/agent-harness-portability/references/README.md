# References index

- `axis-probes.md` — one entry per axis in the disposition table: the probe question to answer per target, how a `verified` answer is obtained, the silent failure the axis produces when it is wrong, and the repair that closes it. Read while filling the table.
- `verification-harness.md` — how the token list is built, the checks that run over it, cross-target uniqueness, freshness of generated trees, and how to confirm a check can fail. Read while running the checks.
