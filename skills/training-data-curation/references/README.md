# References index

- `row-shapes.md` — the row shape each training method expects, the template-application rule, and the flat-text path that trains on everything without erroring.
- `conversion-and-holdout.md` — the holdout filter and its ordering constraint, top-fraction selection, pair selection with the candidate-count floor, and the shaping functions that strip the identifiers.
- `synthetic-and-replay.md` — the generation-method ladder, the filter funnel's ordering rule, replay-mix construction, and the swap-do-not-add rule for changing a replay fraction between runs.
