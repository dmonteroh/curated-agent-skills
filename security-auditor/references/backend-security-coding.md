# Secure Backend Coding (Condensed)

- Validate all inputs at the boundary.
- Use parameterized queries everywhere.
- Enforce authz checks on every endpoint.
- Avoid leaking sensitive info in errors/logs.
- Apply rate limits to auth and expensive endpoints.
